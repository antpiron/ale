This library have been developped for GNU/Linux. While it should work on other unix-like system, the testing is not exhaustive.


How to compile from the git repository:

autoreconf -i

./configure

make -j4

sudo make install

sudo ldconfig /usr/local/lib/
